# Optional: a Hugging Face token raises Hub download rate limits and speeds up
# model/dataset fetches. Not required — the bench runs anonymously.
# HF_TOKEN=hf_...

# The passive rigs (scratch/passive_*.py) call the OpenRouter API.
# OPENROUTER_API_KEY=sk-or-v1-...
